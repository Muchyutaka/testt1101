# prop-overrides v3 (applied by 06-patch-props.sh, in place, .orig backup kept)
# Density everywhere; characteristics ONLY in donor product (the key init reads).
# Fingerprints/identity stay generic for first boot (GMS tuning post-boot).

# all 4 donor build.props (system, system_ext, product, tr_product):
ro.sf.lcd_density=280

# donor product ONLY:
ro.build.characteristics=tablet
